/** Retail kiosk viewport constraints */
export const RETAIL_VIEWPORT = {
  MIN_WIDTH: 1280,
  MAX_WIDTH: 1920,
  HEIGHT: 1020,
  /** Below this viewport height, the full page scrolls vertically */
  PAGE_SCROLL_HEIGHT: 1080,
  BETSLIP_WIDTH: 400,
} as const
